import { Typography } from "@mui/material";
import AdminPageContainer from "components/admin/AdminPageContainer";

const Pesanan = () => {
  return <Typography>pesanan</Typography>;
};

export default Pesanan;
